require 'spec_helper'

describe LogServer do
  it 'has a version number' do
    expect(LogServer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
